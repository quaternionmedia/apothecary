# Playwright Configuration
# See https://playwright.dev/python/docs/test-runners

[pytest]
# Playwright will automatically use these settings
# when pytest-playwright is installed

# Default browser to use
PWDEBUG = 1  # Set to enable Playwright Inspector for debugging

# You can also configure via command line:
# pytest --browser firefox --headed --slowmo 1000
